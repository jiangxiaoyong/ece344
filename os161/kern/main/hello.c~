#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<lib.h>

typedef struct
{
   char a;
   char b;
   char c;

}s1;

int main()
{
	char * buffer;
	char string1[] = "asdf";
	char string2[] = "asdfas";
	printf("asdf");
	buffer = (char *)malloc(sizeof(char)*2);

	s1 string;
	string.b = "adfasdf";

	if(strcmp(string1,string2))
	{
	
	
	}

	kprintf(asdf);
	kprintf("saf");

}
